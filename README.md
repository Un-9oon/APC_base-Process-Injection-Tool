# APC_base-Process-Injection-Tool
This tool is a Process Injection Tool using the APC technique where instead of creating new Threads  ( which are more likely to be flagged by EDR and AVs ) , it Hijacks existing thread and Inject the code in its queue
